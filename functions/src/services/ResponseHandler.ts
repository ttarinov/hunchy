import { injectable } from "inversify";

@injectable()
export class ResponseHandler {
  // This service is kept for future use but proposal parsing is removed
  // AI now calls git_commit tool directly instead of generating markdown proposals
}
